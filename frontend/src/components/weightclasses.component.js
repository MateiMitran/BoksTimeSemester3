import React from "react";
import Slideshow from './Slideshow';
import { SliderData } from './SlideshowData';

 export default function WeightClassesComponent() {
      return (
        <Slideshow slides = {SliderData} />
      );
  }
  